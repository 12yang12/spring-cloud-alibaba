server:
  port: 8091

spring:
  application:
    name: sentinel-dashboard
  cloud:
    sentinel:
      transport:
        port: 8719
        dashboard: 127.0.0.1:8090
        clientIp: 127.0.0.1

management:
  endpoints:
    web:
      exposure:
        include: '*'
